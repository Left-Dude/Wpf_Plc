<UserControl x:Class="Wpf_Plc.ProgramView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="White">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель меню -->
        <StackPanel Grid.Column="0" Background="#1400AA">
            <TextBlock Text="МЕНЮ" Foreground="White" Padding="10" FontFamily="DM Sans" FontWeight="Bold" HorizontalAlignment="Center"/>
            <Border Background="#1400AA" Padding="10" Margin="0,10,0,0" HorizontalAlignment="Left">
                <TextBlock Text="PLC" Foreground="White" FontFamily="DM Sans" FontWeight="Bold"/>
            </Border>
            <Button Content="ПРОГРАММА" Click="BtnPROG_Click" Foreground="White" FontFamily="DM Sans" FontWeight="Bold" Background="Transparent" BorderBrush="Transparent" Padding="10" Margin="0,10,0,0" HorizontalAlignment="Left"/>
            <Button Content="ВЫХОД" Click="BtnEXIT_Click" Foreground="White" FontFamily="DM Sans" FontWeight="Bold" Background="Transparent" BorderBrush="Transparent" Padding="10" Margin="0,10,0,0" HorizontalAlignment="Left"/>
            <ContentControl x:Name="MainContent" Margin="10" />
        </StackPanel>

        <!-- Правая часть -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40,20,40,40" Background="White">

                <!-- Изображение схемы -->
                <Image Source="assets/schema_traffic_lights.png" Stretch="None" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <!-- Линия-разделитель -->
                <Rectangle Height="1" Fill="#CCCCCC" HorizontalAlignment="Stretch" Margin="0,0,0,20"/>

                <!-- Блок: СВЕТОФОР -->
                <TextBlock Text="СВЕТОФОР" Foreground="Black" FontFamily="DM Sans" FontWeight="Bold"
                           FontSize="16" Margin="0,0,0,0"/>

                <!-- Блоки: МИГАНИЕ и БЕГУЩИЙ ОГОНЬ -->
                <TextBlock Text="МИГАНИЕ" Foreground="Black" FontFamily="DM Sans" FontWeight="Bold"
                           FontSize="16" Margin="0,10,0,0"/>

                <!-- Нижний блок с вертикальной линией-разделителем -->
                <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="1"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Левая часть -->
                    <StackPanel Grid.Column="0" VerticalAlignment="Top" Margin="0,0,20,0"/>

                    <!-- Вертикальная линия -->
                    <Rectangle Grid.Column="1" Width="1" Fill="#CCCCCC" Margin="10,0"/>

                    <!-- Правая часть -->
                    <StackPanel Grid.Column="2" Orientation="Vertical" HorizontalAlignment="Right" Margin="20,0,0,0">
                        <!-- Чекбоксы -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <CheckBox Content="RS" FontFamily="DM Sans" FontWeight="Bold" FontSize="14" Margin="0,0,20,0"/>
                            <CheckBox x:Name="EthernetCheckBox" Content="ETHERNET" FontFamily="DM Sans" FontWeight="Bold" FontSize="14" Checked="CheckBox_Checked"/>
                        </StackPanel>

                        <!-- Кнопка ЗАГРУЗИТЬ -->
                        <Button Content="ЗАГРУЗИТЬ" Width="180" Height="40" HorizontalAlignment="Right"
                                Background="#E0E0E0" Foreground="Black" FontFamily="DM Sans" FontWeight="Bold"
                                BorderThickness="0" Click="Button_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="20">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
