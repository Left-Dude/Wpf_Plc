<UserControl x:Class="Wpf_Plc.PlcDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wpf_Plc"
             mc:Ignorable="d" 
             d:DesignHeight="675" d:DesignWidth="1400">

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель: сначала заголовок, потом кнопки, потом MainContent -->
        <Grid Grid.Column="0" Background="#1400AA">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Заголовок -->
                <RowDefinition Height="Auto"/> <!-- Кнопки меню -->
                <RowDefinition Height="*"/>    <!-- Переключаемое содержимое -->
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock Grid.Row="0"
                       Text="МЕНЮ"
                       Foreground="White"
                       Padding="10"
                       FontFamily="DM Sans"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>

            <!-- Список пунктов меню -->
            <StackPanel Grid.Row="1" Margin="0,10,0,0">
                <!-- Текущий раздел подсвечен серым фоном -->
                <Border Background="#989898" Padding="10" HorizontalAlignment="Left">
                    <TextBlock Text="PLC"
                               Foreground="White"
                               FontFamily="DM Sans"
                               FontWeight="Bold"/>
                </Border>
                <Button Content="ПРОГРАММА"
                        Click="BtnPROG_Click"
                        Foreground="White"
                        FontFamily="DM Sans"
                        FontWeight="Bold"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Padding="10"
                        Margin="0,5,0,0"
                        HorizontalAlignment="Left"/>
                <Button Content="ВЫХОД"
                        Click="BtnEXIT_Click"
                        Foreground="White"
                        FontFamily="DM Sans"
                        FontWeight="Bold"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Padding="10"
                        Margin="0,5,0,0"
                        HorizontalAlignment="Left"/>
            </StackPanel>

            <!-- Область для вложенных видов -->
            <ContentControl x:Name="MainContent"
                            Grid.Row="2"
                            Margin="10"/>
        </Grid>

        <!-- Правая часть: тут ваши карточки PLC -->
        <Grid Grid.Column="1" Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Можно добавить заголовок -->
                <RowDefinition Height="*"/>    <!-- Список карточек -->
                <RowDefinition Height="Auto"/> <!-- Кнопка загрузки -->
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="1"
                          Margin="10"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl x:Name="PlcItemsControl"
                              ItemsSource="{Binding PLCModels}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:PlcInfoCard PlcModel="{Binding}" Margin="0,20,0,20"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
